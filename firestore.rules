
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Default to read-only for authenticated users, deny all writes.
    // All write/update/delete operations should be handled by secure Cloud Functions.
    match /{document=**} {
      allow read: if request.auth != null;
      allow write: if false;
    }
  }
}
