rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Default to read-only for authenticated users.
    // All sensitive writes should be handled by Cloud Functions.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
